import {COLORS, FONT_SIZE, FONT} from "../../constants/theme"

export const styles = {
    categoria: {
        width: 100,
        height: 100,
        marginHorizontal: 5,
        // paddingRight: 20,
        borderRadius: 12, 
        alignItems: "center",
        justifyContent: "center",
    },

    icone: {
        width: 40,
        height: 40,
    },

    descricao: {
        fontSize: FONT_SIZE.sm,
        textAlign: "center",
        marginTop: 8,
        fontWeight: 500
    },
}