import {COLORS, FONT_SIZE} from "../../constants/theme"

export const styles = {
    container: {
        width: "100%",
        alignItems: "center"
    },

    logo: {
        width: 262,
        height: 248
    }
}