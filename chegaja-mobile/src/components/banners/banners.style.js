import {COLORS, FONT_SIZE, FONT} from "../../constants/theme"

export const styles = {
    banner: {
        width: 360,
        marginTop: 20,
        marginBottom: 20,
        marginHorizontal: 10,
    },

    icone: {
        width: "100%",
        height: 180,
        borderRadius: 12,
    },

}